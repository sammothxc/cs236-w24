#pragma once
#include <string>
#include <vector>
#include "Token.h"
#include "Parameter.h"

class Predicate {
private:
    Token predicateName;
    std::vector<Parameter> parameters;

public:
    // Constructor, Access Methods, Update Methods, toString, etc.
};